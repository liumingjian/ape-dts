# SPEC — Docs + E2E Script

## Goal

- 更新 `docs/agent-summary/gaussdb-progress-tracker.md`：补齐 `GaussDBOracle -> Oracle` struct/check 入口与 PASS 证据索引。
- 更新 `docs/agent-summary/gaussdb-e2e-test-plan.md`：补齐 quick matrix。
- 更新 `scripts/e2e/oracle_gaussdboracle_bootstrap.sh`：加入新用例。

## Done-When

- `rg` 可检索新入口

