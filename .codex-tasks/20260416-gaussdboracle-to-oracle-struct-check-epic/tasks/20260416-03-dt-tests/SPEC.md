# SPEC — dt-tests Coverage

## Goal

为 `GaussDBOracle -> Oracle` 新增回归入口：

- `struct basic`: `gaussdb_oracle_to_oracle::struct_tests::test::struct_basic_test`
- `check basic`: `gaussdb_oracle_to_oracle::check_tests::test::check_basic_test`

## Done-When

- 两个用例在本机 Oracle XE + 远端 GaussDBOracle 环境可 PASS

